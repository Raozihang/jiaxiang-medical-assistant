# 11｜P0 冒烟验收清单

> 最近更新：2026-02-23  
> 适用阶段：P0（M4 验收）

## 1. 验收目标
- 覆盖核心业务闭环：登记 → 接诊 → 处置 → 离开；
- 覆盖关键支撑能力：库存变更、报表统计、鉴权与错误结构；
- 将可自动化部分沉淀为固定命令，减少回归成本。

## 2. 自动化冒烟（已落地）
### 2.1 后端 API 冒烟
- 用例文件：`backend/internal/bootstrap/p0_smoke_test.go`
- 执行命令：
```bash
cd backend
go test ./internal/bootstrap -run TestP0SmokeFlow -v
```

### 2.2 已覆盖项
- [x] 健康检查接口可用（`GET /api/v1/healthz`）
- [x] 鉴权拦截生效（未登录访问受保护接口返回 401）
- [x] 学生登记创建就诊（`POST /api/v1/visits`）
- [x] 医生查询/更新/查看就诊详情（`GET/PATCH /api/v1/visits`）
- [x] 药品库存扣减流程（`POST /api/v1/medicines/outbound`）
- [x] 管理端总览报表（`GET /api/v1/reports/overview`）
- [x] 错误请求返回标准错误码（`code=1001`）

## 3. 手工冒烟清单（待收口）
### 3.1 前端角色链路
- [x] 学生端：完成登记表单提交并出现成功反馈
- [x] 医生端：列表进入详情并完成诊断更新
- [x] 管理端：报表页、导入页、通知页可正常加载
- [x] 权限页：越权访问跳转 `/forbidden`

### 3.2 联调与体验
- [x] 登录态过期后自动回到 `/login`
- [x] 关键接口失败时页面提示可读错误信息
- [x] 页面懒加载时显示统一 loading 态

## 4. 验收记录模板（每次执行补充）
| 日期 | 执行人 | 范围 | 结果 | 备注 |
|---|---|---|---|---|
| 2026-02-23 | Codex | 后端 API 自动化冒烟 | 通过 | 见 `TestP0SmokeFlow` |
| 2026-02-23 | 项目组 | 前端手工联调冒烟 | 通过 | 角色链路、鉴权、懒加载与异常提示均正常 |
